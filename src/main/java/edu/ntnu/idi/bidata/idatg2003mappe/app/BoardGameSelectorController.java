package edu.ntnu.idi.bidata.idatg2003mappe.app;

public class BoardGameSelectorController {
  BoardGameSelector boardGameSelector = new BoardGameSelector();

  public void switchGame(int gameSelector) {
    boardGameSelector.switchGame(gameSelector);
  }
}
